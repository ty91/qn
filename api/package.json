{
  "name": "cfw",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.4",
    "wrangler": "^4.4.0"
  }
}